node_modules/

# Environment — never commit real secrets
.env
.env.local
.env.*.local
.dev.vars

# Vercel build output
.vercel/

# Wrangler local dev state/cache (includes local D1 SQLite state)
.wrangler/

# macOS
.DS_Store

# Internal SEO audit output — never commit. pages_build_output_dir is "."
# (repo root), so anything committed here is publicly servable at
# thedeangeloseries.com/<path>; this documents real, current weaknesses
# and must stay local-only.
thedeangeloseries.com-audit/
